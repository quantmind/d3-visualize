


//
// Exponential moving average transform
// Useful for smoothing out volatile timeseries
export default function (config) {

    return movingAverage;

    
    function movingAverage (frame) {
        const periods = config.periods || 10;

    }
}
