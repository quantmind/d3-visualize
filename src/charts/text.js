import createChart from '../core/chart';


export default createChart('text', {

    options: {
        scale: 'linear',
        field: 'data',
        label: 'label'
    },

    doDraw () {
    }
});
